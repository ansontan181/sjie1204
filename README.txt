使用说明：
1）把压缩包解压到一个文件夹。
2）先双击打开 index.html，点击按钮会进入 birthday.html。两者必须在同一层目录。
3）如需背景音乐，把你的 mp3 文件放在同一文件夹并重命名为 music.mp3。
4）若本地打不开，请确认：
   - 文件名是否真的叫 index.html / birthday.html（不是 .html.txt）。
   - 两个文件是否放在同一层目录。
   - 从 index.html 进去，或直接双击 birthday.html 也可以。
